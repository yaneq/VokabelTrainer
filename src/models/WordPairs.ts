import { iWordPair } from '../types/types'

const WORD_PAIRS = [
  ['los cubiertos', 'das Besteck'],
  ['los platos', 'die Teller'],
  ['gustar', 'mögen'],
  ['colgar', 'aufhängen'],
  ['aspirar', 'staubsaugen'],
  ['podar', 'Rasen mähen]'],
]

export const getRandomWordPair = (): iWordPair => {
  return
}
